# Monitor-RS Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# Grafana Configuration
GRAFANA_PORT=8080

# Prometheus Configuration
PROMETHEUS_PORT=9090

# Monitor-RS Configuration (running on host)
# The stack will scrape metrics from host.docker.internal:9100
